{
    "name": "@inventorymanager/auth-lambda",
    "private": true,
    "type": "module",
    "sideEffects": false,
    "main": "src/index.ts",
    "scripts": {
        "build": "yarn build:local",
        "build:local": "rm -rf dist && esbuild ./src/local.ts --bundle --target=node22.0.0 --platform=node --outdir=dist --out-extension:.js=.cjs",
        "dev": "yarn build && node ./dist/local.cjs"
    },
    "dependencies": {
        "@aws-sdk/client-cognito-identity-provider": "^3.798.0",
        "@inventorymanager/app-schema": "workspace:*",
        "@inventorymanager/cognito-auth": "workspace:*",
        "aws-lambda": "^1.0.7",
        "dotenv": "^16.5.0",
        "zod": "^3.24.3"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.149",
        "@types/cors": "^2",
        "@types/express": "^5",
        "cors": "^2.8.5",
        "esbuild": "^0.25.3",
        "express": "^5.1.0"
    },
    "volta": {
        "extends": "../../package.json"
    }
}
